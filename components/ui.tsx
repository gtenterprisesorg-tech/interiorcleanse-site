import Link from 'next/link'; import { ReactNode } from 'react';
export function EyebrowLabel({children}:{children:ReactNode}){return <div className="mb-6 flex items-center gap-4 text-[11px] font-medium uppercase tracking-[.18em] text-taupe"><span>{children}</span><span className="h-px flex-1 bg-hairline" /></div>}
export function Button({href,children,variant='primary',external=false}:{href:string;children:ReactNode;variant?:'primary'|'secondary'|'text';external?:boolean}){const cls=variant==='primary'?'inline-flex min-h-12 items-center justify-center bg-sage px-7 py-3 text-sm font-medium uppercase tracking-[.14em] text-white transition hover:bg-ink':variant==='secondary'?'inline-flex min-h-12 items-center justify-center border border-hairline px-7 py-3 text-sm font-medium uppercase tracking-[.14em] transition hover:border-sage hover:text-sage':'inline-flex min-h-11 items-center text-sm font-medium uppercase tracking-[.14em] text-sage underline decoration-hairline transition hover:text-ink'; return external?<a className={cls} href={href} target="_blank" rel="noreferrer">{children}</a>:<Link className={cls} href={href}>{children}</Link>}
export function Section({children,className=''}:{children:ReactNode;className?:string}){return <section className={`mx-auto max-w-7xl px-6 py-24 sm:px-8 lg:py-32 ${className}`}>{children}</section>}
export function DisclosureBox(){return <div className="border border-hairline bg-white/60 p-5 text-sm leading-7 text-taupe">This site contains affiliate links. We may earn a commission if you purchase through links on this page, at no extra cost to you. Checkout is completed securely on Amazon or TikTok Shop — we never collect your payment details on this site.</div>}
