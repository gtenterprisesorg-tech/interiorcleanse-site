import Link from 'next/link'; import { Product, Book, Article } from '@/lib/types'; import { Button, EyebrowLabel } from './ui';
export function ProductCard({product}:{product:Product}){return <article className="group"><Link href={`/shop/${product.slug}`} className="block overflow-hidden bg-white"><img src={product.heroImage} alt={product.imageAlt} className="aspect-[4/5] w-full object-cover transition duration-700 group-hover:scale-[1.03]" /></Link><p className="mt-6 text-[11px] font-medium uppercase tracking-[.18em] text-taupe">{product.category}</p><h3 className="mt-3 font-serif text-2xl font-medium"><Link href={`/shop/${product.slug}`}>{product.name}</Link></h3><p className="mt-3 line-clamp-2 text-sm leading-7 text-taupe">{product.editorialBlurb}</p><Button href={product.tiktokShopUrl} variant="text" external>View on TikTok Shop →</Button></article>}
export function BookCard({book}:{book:Book}){return <article className="grid gap-6"><Link href={`/library/${book.slug}`}><img src={book.coverImage} alt={book.imageAlt} className="mx-auto aspect-[7/10] w-full max-w-xs object-cover shadow-soft transition duration-700 hover:scale-[1.02]" /></Link><div><p className="text-[11px] font-medium uppercase tracking-[.18em] text-taupe">The Library</p><h3 className="mt-3 font-serif text-2xl font-medium"><Link href={`/library/${book.slug}`}>{book.title}</Link></h3><p className="mt-3 text-sm leading-7 text-taupe">{book.hook}</p><div className="mt-5 flex flex-wrap gap-3"><Button href={book.paperbackUrl} variant="secondary" external>Buy Paperback</Button><Button href={book.kindleUrl} variant="text" external>Buy on Kindle</Button></div></div></article>}
export function ArticleCard({article}:{article:Article}){return <article><img src={article.image} alt={article.imageAlt} className="aspect-[16/11] w-full object-cover"/><EyebrowLabel>{article.eyebrow}</EyebrowLabel><h3 className="font-serif text-2xl"><Link href={`/journal/${article.slug}`}>{article.title}</Link></h3><p className="mt-3 text-taupe">{article.excerpt}</p></article>}
