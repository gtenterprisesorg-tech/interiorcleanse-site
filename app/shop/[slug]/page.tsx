import { allProducts, getProduct } from '@/lib/content'; import { notFound } from 'next/navigation'; import { Button, DisclosureBox, EyebrowLabel, Section } from '@/components/ui';
export function generateStaticParams(){return allProducts.map(p=>({slug:p.slug}))}
export default function ProductPage({params}:{params:{slug:string}}){const p=getProduct(params.slug); if(!p) notFound(); return <Section><div className="grid gap-12 lg:grid-cols-2"><div className="grid gap-4">{p.gallery.map(img=><img key={img} src={img} alt={p.imageAlt} className="w-full bg-white object-cover"/> )}</div><div><EyebrowLabel>{p.category}</EyebrowLabel><h1 className="font-serif text-6xl">{p.name}</h1><p className="mt-6 text-lg leading-8 text-taupe">{p.editorialBlurb}</p>{p.priceRange&&<p className="mt-5 text-sm uppercase tracking-[.16em] text-taupe">Typical range: {p.priceRange}</p>}<div className="mt-8"><DisclosureBox/></div><div className="mt-8"><Button href={p.tiktokShopUrl} external>View on TikTok Shop</Button></div><p className="mt-6 text-sm leading-7 text-taupe">TikTok Shop availability and pricing can change. Verify current terms and affiliate eligibility before launch.</p></div></div></Section>}
