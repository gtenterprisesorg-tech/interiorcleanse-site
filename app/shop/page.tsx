import { allProducts } from '@/lib/content'; import { ProductCard } from '@/components/cards'; import { DisclosureBox, EyebrowLabel, Section } from '@/components/ui';
export default function Shop(){const cats=['All',...Array.from(new Set(allProducts.map(p=>p.category)))];return <Section><EyebrowLabel>Shop the Edit</EyebrowLabel><h1 className="max-w-3xl font-serif text-6xl">Curated home-care finds, routed to TikTok Shop.</h1><div className="mt-10"><DisclosureBox/></div><div className="mt-10 flex flex-wrap gap-3">{cats.map(c=><a key={c} href={c==='All'?'/shop':`/shop#${c.toLowerCase().replaceAll(' ','-')}`} className="border border-hairline px-4 py-2 text-xs uppercase tracking-[.14em] hover:border-sage">{c}</a>)}</div><div className="mt-14 grid gap-10 sm:grid-cols-2 lg:grid-cols-3">{allProducts.map(p=><ProductCard product={p} key={p.slug}/>)}</div></Section>}
